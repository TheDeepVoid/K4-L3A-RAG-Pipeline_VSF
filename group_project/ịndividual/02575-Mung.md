# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Nguyễn Thị Mừng
- Mã học viên: 2A202602575
- Nhóm: VSF
- Repository/branch: https://github.com/TheDeepVoid/K4-L3A-RAG-Pipeline_VSF — branches `NguyenThiMung-task5,6`, `feat/task5-6-retrieval`, `feat/full-rag-with-db` (đã merge về main)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 & 2 — Dữ liệu nguồn (thu thập/crawl) | Đưa corpus chính của pipeline vào repo: 3 văn bản pháp luật (Luật 108/2025/QH15 ~3.4MB, Luật 67/2025/QH15, dự thảo Luật Thuế) và 5 bài báo thuế JSON đủ `url/title/date_crawled/content_markdown`, kèm bản standardized markdown (5 legal + 5 news) dùng cho chunking/indexing | `data/landing/legal/`, `data/landing/news/`, `data/standardized/`; commit `c83ea90` (branch `feat/task5-6-retrieval`) | Done |
| Task 5 — Semantic search | Triển khai `semantic_search(query, top_k)` từ stub `NotImplementedError`: embed query bằng `embed_texts()` chung của Task 4, query ChromaDB cosine, đổi distance→similarity, trả SearchResult `method=dense` sort giảm dần, xử lý response rỗng | `src/task5_semantic_search.py`; commits `565c54a` (branch `NguyenThiMung-task5,6`), `c83ea90` | Done |
| Task 6 — Lexical search (BM25) | Triển khai `build_bm25_index()` bằng BM25Plus và `lexical_search(query, top_k)`: corpus dùng chung chunks với Task 5 (nạp lazy từ Chroma hoặc seed bởi `task4.run_pipeline`), lọc score ≤ 0, trả `[]` khi DB rỗng/lỗi | `src/task6_lexical_search.py`; commits `565c54a`, `c83ea90`, `93c4528` | Done |
| Ứng dụng Streamlit (khởi tạo) | Dựng UI chat hỏi đáp: lưu hội thoại trong `session_state`, sidebar `top_k`, spinner, gọi `generate_with_citation` và render câu trả lời thay cho stub "RAG Chatbot" | `app.py`; commit `15b97ed` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Task 5 dùng chung hàm embedding của Task 4 (`embed_texts`/`get_collection`) thay vì tự duy trì embedding riêng.  
   **Lý do/evidence:** Query embedding và chunk embedding phải cùng model/dimension mới query Chroma được; source import `from .task4_chunking_indexing import embed_texts, get_collection`, và `tests/test_contracts.py::test_semantic_search_uses_shared_embedding_and_contract` kiểm tra đúng hành vi này.  
   **Trade-off:** Task 5 gắn chặt Task 4 — khi đổi provider/dimension embedding phải rebuild index đồng bộ (đã từng vỡ khi PR #7 đổi task4 sang model khác).

2. **Quyết định:** Task 6 dùng BM25Plus trên **cùng corpus chunks** với Task 5, nạp lazy từ Chroma để khỏi đọc lại toàn bộ dữ liệu mỗi lần search.  
   **Lý do/evidence:** Docstring task6 ghi rõ nạp lazy + seed bởi `task4.run_pipeline()` (`task6.CORPUS = chunks`); Task 7 (RRF) cần dense và BM25 chia sẻ cùng tập `id`; `tests/test_contracts.py::test_lexical_search_returns_bm25_contract` kiểm tra corpus rỗng trả `[]`.  
   **Trade-off:** Tokenize đơn giản (`lower().split()`) chưa tách từ tiếng Việt nên dễ miss từ ghép; index bị rebuild lại toàn bộ khi corpus đổi kích thước.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: chạy `semantic_search("test query", top_k=3)` và `lexical_search("test query", top_k=3)` qua `__main__`; đối chiếu contract test `test_semantic_search_uses_shared_embedding_and_contract`, `test_lexical_search_returns_bm25_contract` trong `tests/test_contracts.py`; chạy `pytest`.
- Kết quả trước/sau nếu có: Task 5–6 ban đầu là stub ném `NotImplementedError` → sau khi triển khai trả `SearchResult` đúng schema (`retrieval_method=dense/bm25`, score giảm dần, ≤ `top_k`); app.py từ stub "RAG Chatbot" → UI hỏi đáp Luật & Thuế hoàn chỉnh.
- Lỗi đã phát hiện và cách xử lý: Chroma rỗng/query lỗi → kiểm tra `ids[0]` trống và trả `[]` thay vì crash; BM25 gặp corpus rỗng/lỗi DB → trả `[]`; score ≤ 0 bị lọc khỏi kết quả.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: BM25 tokenize chưa tách từ tiếng Việt và semantic search cần index Task 4 đã build sẵn (phải chạy `task4.run_pipeline()` hoặc có `chroma_db` trước).
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: thử Vietnamese word segmentation (pyvi/VnCoreNLP) hoặc n-gram cho BM25, và thêm test end-to-end cho luồng hybrid (dense + BM25 → RRF).

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/9/2026
- Tên thành viên: Nguyễn Thị Mừng