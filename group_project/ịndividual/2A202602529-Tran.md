# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Bùi Thị Ngọc Trân
- Mã học viên: 2A202602529
- Nhóm: VSF
- Repository/branch: TheDeepVoid/K4-L3A-RAG-Pipeline_VSF — trantran2929

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 – Thu thập tài liệu | Cấu hình nguồn tải 3 tài liệu luật/dự thảo dạng DOCX; lưu tài liệu gốc để xử lý tiếp. | src/task1_collect_legal_docs.py; data/landing/legal/ | Done |
| Task 2 – Crawl bài viết | Cấu hình 5 URL; dùng Crawl4AI thu thập bài viết và lưu JSON gồm URL, tiêu đề, thời điểm crawl, nội dung Markdown. | src/task2_crawl_news.py; data/landing/news/ | Done |
| Task 3 – Chuẩn hóa dữ liệu | Chuyển tài liệu pháp lý bằng MarkItDown; chuyển JSON bài viết sang Markdown, giữ thông tin nguồn và thời điểm crawl ở đầu bài. | src/task3_convert_markdown.py; data/standardized/ | Done |
| Task 4 – Chunking, embedding, indexing | Chia văn bản thành chunks; tạo embedding; upsert nội dung, vector và metadata vào ChromaDB; xử lý lỗi dùng sai model embedding. | src/task4_chunking_indexing.py | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:**  Chuẩn hóa các nguồn về Markdown, giữ hai nhóm legal và news.
   **Lý do/evidence:**   Task 4 có thể đọc các nguồn qua một luồng xử lý chung; dữ liệu đầu ra nằm trong data/standardized/.
   **Trade-off:** Chuyển đổi có thể làm mất một phần định dạng bảng hoặc bố cục của tài liệu gốc.

2. **Quyết định:**  Chia đệ quy với chunk_size=500 và chunk_overlap=50 ký tự; tạo ID từ đường dẫn tài liệu và chỉ số chunk, sau đó upsert.
   **Lý do/evidence:** Giới hạn độ dài đoạn và giữ một phần ngữ cảnh ở ranh giới; ID ổn định giúp cập nhật cùng chunk khi chạy lại trên dữ liệu không đổi.
   **Trade-off:** Overlap làm tăng dữ liệu và chi phí embedding; đoạn 500 ký tự có thể chia cắt điều khoản dài. Các tham số chưa được tối ưu bằng đánh giá so sánh.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Chạy `python -m src.task4_chunking_indexing` để kiểm tra luồng đọc Markdown, chia chunk, tạo embedding và lưu vào ChromaDB.
- Kết quả trước/sau nếu có: Ban đầu, tôi dừng chương trình ở bước tạo embedding do dùng SentenceTransformer để tải model text-embedding-3-small từ Hugging Face, dẫn đến lỗi 404. Sau khi chuyển sang gọi OpenAI API, chương trình chạy thành công và báo `Indexed 1404 chunks`. Kết quả này được ghi nhận trước lần merge cuối; chưa xác nhận lại trên phiên bản sau merge.
- Lỗi đã phát hiện và cách xử lý: `SentenceTransformer` tìm model `text-embedding-3-small` trên Hugging Face trong khi đây là model của OpenAI. Đã điều chỉnh cách gọi embedding để sử dụng model đúng với provider. Chưa thực hiện kiểm thử tự động hoặc đánh giá định lượng chất lượng truy xuất.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Task 4 chưa đưa URL nguồn vào metadata của chunk; trường URL hiện để trống. Vì vậy, việc truy xuất đường dẫn gốc từ metadata còn hạn chế, dù Markdown bài viết đã lưu thông tin nguồn.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Truyền URL nguồn từ bước thu thập và chuẩn hóa vào metadata của chunk, sau đó kiểm tra khả năng truy vết từ kết quả tìm kiếm về tài liệu gốc.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Bùi Thị Ngọc Trân
