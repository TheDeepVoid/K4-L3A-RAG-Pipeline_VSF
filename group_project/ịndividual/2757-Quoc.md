# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Ngô Gia Quốc
- Mã học viên: 2A202602757
- Nhóm: VSF
- Repository/branch: https://github.com/TheDeepVoid/K4-L3A-RAG-Pipeline_VSF/tree/NgoGiaQuoc-02757 

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 9 — Retrieval pipeline | Kết hợp dense search và BM25 bằng RRF; dùng cosine score gốc để quyết định PageIndex fallback; xử lý lỗi fallback để pipeline không bị crash. | `src/task9_retrieval_pipeline.py` | Done |
| Task 10 — Generation có citation | Reorder chunks để giảm lost-in-the-middle; format context kèm ID/title/source/URL; hỗ trợ OpenAI, Gemini, Anthropic; trả safe refusal khi thiếu evidence hoặc provider lỗi. | `src/task10_generation.py` | Done |

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Chỉ dùng cosine score gốc của dense retrieval để kích hoạt PageIndex fallback, không dùng RRF score.  
   **Lý do/evidence:** Dense score phản ánh độ tương đồng semantic, còn RRF score chỉ phản ánh thứ hạng và có thang đo khác; cách làm tuân thủ `docs/MODULE_CONTRACTS.md`.  
   **Trade-off:** Ngưỡng `0.3` cần được hiệu chỉnh lại trên corpus và tập query thực tế.

2. **Quyết định:** Dùng chunk ID ổn định trong citation và triển khai safe refusal cho toàn bộ lỗi retrieval/generation.  
   **Lý do/evidence:** Citation có thể đối chiếu trực tiếp với `sources`; lỗi PageIndex hoặc LLM không làm chatbot crash hay sinh câu trả lời thiếu căn cứ.  
   **Trade-off:** Bắt lỗi ở tầng ngoài giúp hệ thống an toàn nhưng chưa cung cấp log chi tiết để chẩn đoán provider.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Đối chiếu các trường hợp trong `tests/test_contracts.py`: dense score thấp gọi fallback, RRF chỉ chạy một lần, PageIndex lỗi vẫn trả hybrid result, reorder không mutate input và context giữ title/source.
- Kết quả trước/sau nếu có: Trước khi triển khai, Task 9–10 ném `NotImplementedError`; sau khi triển khai đã có đầy đủ retrieval orchestration, provider dispatch và `GenerationResult` đúng schema.
- Lỗi đã phát hiện và cách xử lý: Xử lý query rỗng, `top_k <= 0`, retrieval rỗng, PageIndex/LLM lỗi và output LLM rỗng bằng kết quả rỗng hoặc safe refusal phù hợp.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Chưa hậu kiểm tự động rằng mọi citation do LLM sinh ra đều tồn tại trong `sources`; chạy end-to-end còn phụ thuộc Task 4–8 và API key.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Thêm bộ kiểm tra citation, logging lỗi provider an toàn và hiệu chỉnh `SCORE_THRESHOLD` bằng query trong/ngoài domain.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày:
- Tên thành viên:
